높음 광역어 2차 게이트가 여전히 “실시간 DB 검색어”가 아니라 “프로세스 시작 시 읽은 env 검색어”를 기준으로 동작합니다. app/services/scheduler.py:404-406은 settings.site_keywords를 보고 supporting 키워드를 판정하는데, 실제 검색어 추가/삭제와 동기화는 DB 기준입니다 (app/repositories/sites.py:12-20,55-102, app/slack_app.py:219-251). 그리고 settings.site_keywords는 시작 시 .env에서 1회만 로드됩니다 (app/config.py:84-105,174-180). 그래서 Slack에서 검색어를 바꾸거나 sync_site_keywords()로 DB를 맞춰도, 양식/유지관리/유지보수/통합관리 판정은 재시작 전까지 stale 상태일 수 있습니다. 이 문서가 고친 핵심 버그가 운영 중 다시 재현될 수 있는 구조입니다.

중간 수동 검색 경로는 제외어 필터를 아예 타지 않습니다. app/services/pipeline.py:80-103에는 excluded_scope_reason() 호출이 없고, 채용/마감/relevance만 검사합니다. 반면 문서는 제외어 수정이 시스템 필터 수정처럼 서술합니다 (docs/keyword-filter-bugfix-2026-06-08.md:63-65). 결과적으로 정기 스케줄러에서는 빠지는 공고가 Slack 수동검색/운영 검증에서는 다시 보일 수 있어서, 운영자가 “정책이 안 먹는다”고 오판할 가능성이 큽니다.

중간 실제 배포된 제외어 세트가 문서보다 더 넓습니다. 문서는 #4를 “본문 전체 검사에서 제목 한정으로 축소”로 설명하지만 (docs/keyword-filter-bugfix-2026-06-08.md:63-65,76-84), 라이브 코드 기본 제외어에는 잔디, 상수도, 하수관로, 관망정비, 교육생, 인턴, 채용도 포함돼 있습니다 (app/services/business_scope_filter.py:26-32). 이 추가 제외어들은 문서의 검증 표에 없어서, 제목에 해당 단어가 들어간 관련 공고가 조용히 누락될 위험이 남아 있습니다.

문서와 실제 반영 상태를 맞춰 본 결과, broad_search_terms의 정확일치 수정은 들어가 있고 (app/services/broad_search_terms.py), AI + 기상/해양/양식 제거 후 g2b 활성 검색어가 50개인 것도 확인됐습니다. 문서의 테스트 절차도 .venv/bin/python 기준으로는 실제로 21건 통과했습니다. 다만 위 3가지는 운영 중 다시 문제를 만들 수 있는 부분이라, “문서상 변경은 반영됐지만 시스템적으로 완전히 안전하다”고 보기는 어렵습니다. 수정은 하지 않았습니다.

메모리 인용 1개
오후 1:42
그럼 수정해야 할거 플랜만 새워줘.
오후 1:45

14s 동안 작업
수정 플랜은 이 순서가 맞습니다.

광역어 판정 기준을 env가 아니라 현재 DB의 활성 검색어 기준으로 바꿉니다.
양식/유지관리/유지보수/통합관리의 2차 게이트가 재시작 여부와 무관하게 항상 현재 운영 검색어 상태를 보게 만드는 단계입니다.

정기 스케줄러와 수동검색이 같은 필터를 쓰도록 통합합니다.
지금은 스케줄러와 수동검색의 동작이 다를 가능성이 있어서, 제외어/광역어/관련성 판정을 한 경로로 맞춰야 합니다.

제외어 설정 소스를 하나로 정리합니다.
운영에서 실제 쓰는 EXCLUDED_SCOPE_KEYWORDS 기준으로 코드, .env, 문서, 테스트 예시를 모두 맞춰서 “문서엔 없는데 실제로는 막히는 단어”가 없게 해야 합니다.

검색어 동기화 절차를 명시적으로 만듭니다.
.env를 바꿨을 때 DB site_keywords도 같이 맞춰지는 공식 절차를 만들고, 가능하면 부트스트랩이나 별도 sync 명령으로 강제 일치시키는 게 좋습니다.

회귀 테스트를 보강합니다.
최소한 아래 3가지는 있어야 합니다.

양식/통합관리/유지보수가 supporting 키워드와 함께 통과하는 케이스
수동검색에서도 제외어가 동일하게 적용되는 케이스
DB 검색어를 바꾼 뒤 재시작 없이도 판정이 일관된 케이스
운영 검증을 마지막에 합니다.
배포 후 09:00, 15:00 실행 로그와 Slack 결과를 보고
양식/통합관리 계열이 실제로 통과하는지
조경/도로/폐기물/차량류가 빠지는지
g2b 사이트당 10건 제한이 유지되는지
확인하면 됩니다.
원하면 다음 턴에 이 플랜을 기준으로 “작업 순서 + 위험도 + 예상 영향”까지 붙여서 더 실행용으로 쪼개드리겠습니다.