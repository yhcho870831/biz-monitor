# PC 재부팅 실패 진단 기록

작성일: 2026-06-23

## 목적

이 문서는 해당 PC에서 `sudo reboot` 또는 일반 재부팅 시 시스템이 정상적으로 다시 시작되지 않고 전원 꺼짐 상태로 끝나는 문제에 대해 지금까지 수행한 테스트와 판단을 정리한 기록이다.

## 요약 결론

현재까지의 증거상 문제는 OS가 재부팅 시퀀스를 제대로 시작하지 못하는 문제가 아니라, OS 종료가 완료된 뒤 최종 전원 전환 단계에서 메인보드/BIOS/EC/펌웨어 쪽이 재시작 대신 power-off 상태로 들어가는 문제일 가능성이 높다.

i915 Intel 그래픽 드라이버는 화면 출력 이상의 후보로는 남아 있지만, 재부팅이 전원 꺼짐으로 끝나는 현상의 주원인으로 보이지 않는다.

## 관찰된 증상

- 재부팅 명령 실행 후 화면 마지막에 `power off` 계열 메시지가 표시되고 시스템 전원이 꺼진다.
- 시스템이 자동으로 다시 켜지지 않아 사용자가 물리 전원 버튼을 눌러야 부팅된다.
- 일부 테스트에서는 SSH가 약 39초 후 자동 복귀한 정상 재부팅 사례도 있었으나, 이후 여러 테스트에서 power-off 종료가 반복되었다.
- 컨테이너와 서비스는 부팅 후 정상적으로 올라왔다.

## 시스템/환경 정보

- OS: Linux, 커널 `6.17.0-35-generic`
- 장비: GIGABYTE BRi7(H)-10710 계열
- BIOS: 기존 F3에서 F4로 업데이트 완료
- 주요 컨테이너: `biz-monitor`, `openclaw-cron-google`, `openclaw-agent`

## 테스트 기록

### 1. 기본 재부팅 재현 테스트

첫 번째 재현 테스트에서는 재부팅 후 SSH가 약 39초 뒤 자동으로 복귀했다. 이 결과만 보면 화면만 나오지 않고 장비는 살아 있는 경우가 가능했다.

하지만 두 번째 재현 테스트에서는 재부팅 직후 장비가 꺼졌고, 사용자가 전원 버튼을 눌러야 다시 올라왔다. 이 시점부터 문제는 단순 화면 출력 문제가 아니라, 실제 재시작이 power-off로 끝나는 케이스가 존재한다고 판단했다.

### 2. i915 비활성화 테스트

일회성 부팅 파라미터로 i915를 배제했다.

확인된 커널 파라미터:

```text
module_blacklist=i915 nomodeset
```

커널 로그에서도 i915가 블랙리스트 처리된 것이 확인되었다.

이 상태에서도 호스트와 주요 컨테이너는 정상 기동했다. 그러나 i915 비활성 테스트로 들어가기 직전의 재부팅 역시 전원 꺼짐으로 끝났기 때문에, 재부팅 실패의 주원인은 i915가 아니라 보드/EC/펌웨어 전원 전환 쪽일 가능성이 커졌다.

### 3. i915 블랙리스트 제거 후 기본 부트 확인

테스트용 GRUB 엔트리와 i915 비활성 설정을 제거하고 기본 부팅으로 돌아가도록 했다.

이후 현재 부트의 `/proc/cmdline`에서 `module_blacklist=i915`와 `nomodeset`이 제거된 것을 확인했다.

결과적으로 i915를 다시 로드 가능한 기본 상태로 되돌렸지만, 재부팅 시 power-off로 끝나는 문제는 계속 재현되었다.

### 4. BIOS 업데이트

GIGABYTE BRi7(H)-10710 장비의 BIOS를 F3에서 F4로 업데이트했다.

업데이트 후에도 `sudo reboot` 실행 시 정상 재부팅이 아니라 power-off 상태로 끝났고, 사용자가 전원 버튼을 눌러야 다시 부팅되었다.

따라서 BIOS 업데이트만으로는 문제가 해결되지 않았다.

### 5. `reboot=` 커널 파라미터 테스트

재부팅 방식을 바꾸기 위해 `/etc/default/grub`의 `GRUB_CMDLINE_LINUX_DEFAULT` 값을 여러 방식으로 변경하고 `sudo update-grub` 후 재부팅을 반복했다.

테스트한 값:

```text
reboot=hard
기본값, reboot 파라미터 없음
reboot=acpi
reboot=bios
reboot=efi
reboot=pci
```

각 테스트에서 `/proc/cmdline`을 통해 적용 여부를 확인했다.

모든 경우 공통적으로 OS는 정상적인 종료/재부팅 시퀀스까지 도달했지만, 실제 하드웨어 동작은 재시작이 아니라 전원 꺼짐으로 끝났다.

### 6. 로그 확인

직전 부트 로그는 주로 다음 방식으로 확인했다.

```bash
journalctl -b -1 -k --no-pager
```

필요 시 `systemd-reboot.service`, `reboot.target`, `power`, `shutdown`, `i915` 등의 키워드로 필터링했다.

반복적으로 확인된 점:

- `systemd-reboot.service` 단계까지 진입했다.
- `reboot.target` 도달이 확인되었다.
- Journal 종료 메시지까지 정상적으로 남았다.
- OS 관점에서는 재부팅 종료 절차가 비정상 중단된 흔적이 뚜렷하지 않았다.
- 최종 하드웨어 전원 전환 단계에서 재시작 대신 전원 꺼짐으로 넘어가는 것으로 보인다.

## 현재 판단

소프트웨어 레벨에서 할 수 있는 대표적인 재부팅 방식 테스트는 대부분 수행했다.

`reboot=hard`, `reboot=acpi`, `reboot=bios`, `reboot=efi`, `reboot=pci`, 그리고 기본값 모두에서 동일하게 power-off 현상이 반복되었기 때문에, Linux 커널의 특정 재부팅 방식 선택 문제로 보기 어렵다.

BIOS를 최신 F4로 업데이트한 뒤에도 동일하므로, 남은 가능성은 다음 쪽에 무게가 실린다.

- 메인보드/BIOS/EC의 재시작 전원 전환 버그
- BIOS 전원 관리 옵션 조합 문제
- 특정 하드웨어 장치와 펌웨어 간 ACPI/전원 상태 전환 호환성 문제
- 전원 공급 장치, 어댑터, 보드 전원부의 동작 특성

## 남은 확인 사항

### GRUB 설정 원복

마지막 테스트가 `reboot=pci`였으므로, 추가 테스트가 끝났다면 `/etc/default/grub`의 `GRUB_CMDLINE_LINUX_DEFAULT`에서 `reboot=pci`를 제거하고 기본값으로 되돌리는 것이 좋다.

예상 형태:

```text
GRUB_CMDLINE_LINUX_DEFAULT=""
```

변경 후에는 다음 명령으로 GRUB 설정을 반영해야 한다.

```bash
sudo update-grub
```

### BIOS 전원 관리 옵션 확인

BIOS에서 다음 항목을 확인하는 것이 좋다. 메뉴 이름은 BIOS 버전에 따라 다를 수 있다.

- `Restore on AC Power Loss`: 가능하면 `Power On` 또는 유사 옵션으로 설정
- `ErP`: 비활성화 권장
- `Fast Boot`: 비활성화 권장
- `Wake on LAN`: 필요 시 활성화
- 전원 관련 `Deep Sleep`, `S5`, `EuP` 계열 옵션: 비활성화 후보

특히 원격 운영 장비라면 재부팅 실패 시 전원이 꺼져도 전원 복구 후 자동 부팅되도록 `Restore on AC Power Loss` 옵션을 켜는 것이 중요하다.

## 운영상 권장 대책

이 문제는 OS 명령만으로 안정적으로 해결되기 어려운 하드웨어/펌웨어 계층 문제로 보인다.

운영 장비라면 다음 대책을 고려한다.

- BIOS 전원 복구 옵션을 `Power On`으로 설정
- 스마트플러그, 원격 PDU, UPS 등 원격 전원 제어 장치 사용
- 장애 발생 시 전원 차단/복구로 자동 부팅 가능한지 검증
- 동일 모델 또는 동일 BIOS 버전에서 같은 증상이 보고되는지 제조사 문의
- 장비 교체 또는 다른 보드로 이전 검토

## 최종 상태

현재까지의 테스트 결과만 놓고 보면, i915는 화면 문제 후보로만 남기고 재부팅 실패 문제의 핵심 원인에서는 제외해도 무리가 없다.

재부팅 실패의 핵심은 OS 종료 이후 재시작 신호가 실제 하드웨어에서 power-off로 처리되는 점이며, 이후 조사는 BIOS 전원 옵션과 하드웨어/펌웨어 대응 쪽으로 진행하는 것이 합리적이다.
